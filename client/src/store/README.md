# Store Migration Staging

This folder is created in Phase 1 for the eventual Redux migration.
Current reducers and slices still live under `src/reducer` and `src/slices` to avoid runtime breakage during structural migration.
